import os
import socket
import threading
import hashlib
import pickle
from tqdm import tqdm
from time import sleep

CHUNK_SIZE = 1024 * 1024
TRACKER_PORT = 5000
PEER_PORT_BASE = 6000

class Tracker:
    pass

class Peer:
    pass
